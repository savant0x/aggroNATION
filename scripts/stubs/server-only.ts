/**
 * Stub for the `server-only` guard package, used ONLY by scripts running
 * outside Next.js (via scripts/tsconfig.json path mapping). The real
 * `server-only` package throws when imported outside a React Server
 * Component bundler context; these scripts legitimately run server-side
 * code directly.
 */
export {};
