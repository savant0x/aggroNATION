# --- Supabase (FID-2026-0904-010 — current backend) ---
# Project: aggroNATION-app (ref xunlxdvlhfxokxjnxgrp)
# Dashboard: https://supabase.com/dashboard/project/xunlxdvlhfxokxjnxgrp
# Public identifiers — safe for client bundles.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side (never expose to client). Service role bypasses RLS — guard it.
SUPABASE_SERVICE_ROLE_KEY=

# Direct DB access (migrations, one-off SQL via the pooler).
SUPABASE_DB_PASSWORD=

# --- Firebase (retired per FID-2026-0904-010; still referenced by the one-off
# Firestore→Supabase data migration script until it has run) ---
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# --- Server-side (never expose to client) ---
YOUTUBE_API_KEY=
CRON_SECRET=
GITHUB_TOKEN=
