# Session Summary — 2026-09-03 (FIDs 004–005 implementation)

## Scope in effect
All six FIDs approved (Amendment 2); operator directed completion of remaining
FIDs in logical order. FID-004 (auth) before FID-005 (admin CRUD) — 005
depends on 004's requireAdmin.

## Implemented
- FID-004: `lib/auth/session.ts` (verifySessionCookie, AuthError 401/403
  distinction), `/api/auth/session` (ID-token exchange → 7-day httpOnly
  session cookie, users/{uid} ensure), `/api/auth/logout`, `/api/auth/me`,
  `/app/(auth)/login/page.tsx` (email/password + Google via modular web SDK,
  human-readable error mapping), `scripts/promote-admin.ts` (custom claim
  bootstrap).
- FID-005: `/api/admin/sources` (GET list, POST create with 409 duplicate),
  `/api/admin/sources/[id]` (PATCH partial, DELETE soft-only), `archived`
  schema field added to sources.

## Fixes/en Route
- Modular Auth API functions (`signInWithEmailAndPassword(auth, …)`) — the
  method-style calls in the first draft don't exist in the modular SDK.
- Emulator-mode detection extended to FIREBASE_AUTH_EMULATOR_HOST /
  FIRESTORE_EMULATOR_HOST; `NEXT_PUBLIC_USE_FIREBASE_EMULATORS` documented.
- v3 API verified from docs before use (TextField/Label/Input composition,
  Alert compound anatomy).

## Verification evidence
- Static: type-check, lint (0 errors), build clean — routes registered:
  `/login`, `/api/auth/{session,logout,me}`, `/api/admin/sources[/[id]]`.
- Live boundary probes (dev server :3210):
  - admin GET/POST without session → 401 Authentication required
  - `/api/auth/me` without session → 401
  - cron without bearer → 401
  - session exchange with garbage token → 401 Invalid or expired credentials
    (real Firebase verification)
- Reachability: requireAdmin used by /me + both admin routes; source-repo
  imported by both admin routes (closes FID-002 interim note).

## BLOCKED (presented to operator) — RESOLVED same day
Operator chose option (d), not listed: convert the firebase-tools CLI refresh
token to a standard ADC file (no installs). Unblocked chain:
- ADC file written to gcloud's well-known path → Admin SDK authenticates
- Firestore database created (CLI couldn't `projects:create` provision it);
  Firestore + Identity Toolkit APIs enabled via Service Usage
- Rules + composite indexes deployed live (`firebase deploy`)
- Operator activated Firebase Auth in console (Email/Password + Google)
- CRON_SECRET generated → cron webhook verified live (401/401/200)

## RESOLUTION (addendum, 2026-09-03)
Full dynamic verification completed:
- FID-002: 13/13 dynamic checks (3 real bugs found + fixed, incl. the
  absent-field equality-query trap)
- FID-003: webhook auth + full service cycle pass
- FID-004/005: `scripts/auth-crud-verify.ts` **14/14 PASS** — sign-in →
  session (real RS256 Firebase session JWT) → 403 gate → promote → admin CRUD
  (201/409/422/PATCH/soft-delete) → cleanup. Two test-only bugs fixed (SameSite
  case assertion; idempotent cleanup).
Only outstanding: live YouTube fetch (needs YOUTUBE_API_KEY) + prod deploy.
